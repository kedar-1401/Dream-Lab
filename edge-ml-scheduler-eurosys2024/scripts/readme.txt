Change model name variable and should run provided you have created and mentioned data paths. Required obs data for a given model, refer to data_gen for data_final scripts.

While Running the GMD approach, you will face error if you want to run particular function separately. Main function is the exection of GMD in each of the methods so the functions like fit_lines, return_initial_samples and bs are executable via nested main function only,so run this first.
